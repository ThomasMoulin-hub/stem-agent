# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for stem-agent.
# Team overrides:     {project-root}/_bmad/custom/stem-agent.toml
# Personal overrides: {project-root}/_bmad/custom/stem-agent.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "stem-agent"
name = ""
title = "Evolution Specialist"
icon = "🧬"
description = "A self-specializing agent that evolves based on project signals."
agent_type = "autonomous"   # stateless | memory | autonomous
